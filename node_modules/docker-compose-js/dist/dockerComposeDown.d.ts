import { CommandResult } from './executeCommand';
import { DockerComposeConfigBase } from "./dockerComposeConfigBase";
export interface DockerComposeDownConfig extends DockerComposeConfigBase {
    removeImages?: boolean;
}
export declare function dockerComposeDown(config: DockerComposeDownConfig): Promise<CommandResult>;
