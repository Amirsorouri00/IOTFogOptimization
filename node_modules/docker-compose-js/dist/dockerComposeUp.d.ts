import { CommandResult } from './executeCommand';
import { DockerComposeConfigBase } from "./dockerComposeConfigBase";
export interface DockerComposeUpConfig extends DockerComposeConfigBase {
    servicesToStart?: string[];
    build?: boolean;
    showConfig?: boolean;
}
export declare function dockerComposeUp(config: DockerComposeUpConfig): Promise<CommandResult>;
